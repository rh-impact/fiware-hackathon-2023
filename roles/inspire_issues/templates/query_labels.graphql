query {
	organization(login:"rh-impact") {
		id
		repository(name:"{{ ANSIBLE_PROJECT.GITHUB_REPO_NAME | default(GITHUB_REPO) }}") {
			id
			createdAt
			description
			homepageUrl
			name
			updatedAt
			url
			visibility
			labels(query:"{{ MODEL_NAME }}", first:100) {
				nodes {
					id
					color
					createdAt
					description
					isDefault
					name
					url
				}
			}
		}
	}
}
